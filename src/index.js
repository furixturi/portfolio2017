import React from 'react';
import ReactDOM from 'react-dom';

const title = "Xiaoli Shen | Portfolio 2017";

ReactDOM.render(
    <div>{title}</div>,
    document.getElementById('app')
);

module.hot.accept();